# CodeQuestPlanet 模型交接文件

更新：2026-09-20。项目目录：`/Users/jack/Documents/Codex/CodeQuestPlanet`。

本文件供接手模型快速定位现状和下一单任务。用户最新要求优先于旧工单；不要根据旧版本截图或旧验收报告恢复已经取消的交互。

## 给老师：怎么交给另一个模型

**仍在这个项目里工作：**切换模型或在同一项目新开任务，把下方“首次任务指令”复制给它即可。文件就在项目里，不需要重新上传整段聊天记录。

**换到其他软件或电脑：**交付当前工作目录的源码副本，保留隐藏目录 `.scratch/`、本文件、`AGENTS.md`、`CONTEXT.md`、`docs/`、`signal-runner-node/`、`backend/`、`scripts/` 和项目配置。当前很多代码和文档尚未提交，部分是未跟踪文件，**只给 Git 仓库地址或最后一次提交会缺少这轮成果**。依赖缓存和构建产物无需携带，账户密钥、`.env`、学生数据也不要放进交接包。

如果只能上传文档，先给本文件和下面的规格文件；但仅凭文档无法接着修改真实项目，实施模型仍需要访问当前源码。

## 现在做到哪里

- 32 课已有 v1.8 规划和逐课工单；**第 1–5、17、19 课已按新方案完成技术验收，其余 25 课尚未逐课升级**。这不代表旧课程不存在。
- 第 19 课当前版本是 `1.8-19.5`，不是历史的 19.1–19.4。已恢复原编程卡片、原双码头地图、左图右操作，以及两阶段流程。
- 已有结构化课程接入、真实 Python 执行、版本化学习证据和部分地形基础。第 17 课接入可恢复采集尝试、真实变量事件和计数门；台阶用于第 17、19 课；传送和开关已有基础规则，但相应课程仍待实施验收。
- 第 1、2 课当前版本为 `1.8-01.1`、`1.8-02.1`；第 3 课 `1.8-03.1` 已完成双路线成本与台阶教学；第 4 课 `1.8-04.1` 已完成两处不同首次偏离的运行—定位—替换流程；第 5 课 `1.8-05.1` 已完成坐标选格、断岛显式传送与朝向保持。
- 当前记录：Python 65 个场景、旧课程 345 个场景、v1.8 101 组测试，以及生产构建的 66 个内容哈希资源通过。浏览器完成第 3–5 课全部两阶段、典型错误、刷新恢复和窄屏检查，并回归第 1、2、17、19 课。详见逐课验收记录。
- 用户尚未确认第 1–5、17、19 课的教学效果。真实学生试教、整课时长、云端多设备联调尚未验证。不要宣称“32 课已完成”或“已证明学生掌握”。
- 当前工作区有大量未提交修改。先看 `git status --short`，保留它们；不要 reset、覆盖或清理。也不要顺手处理其他目录和无关页面。

## 用户已经明确的产品要求

1. **学生亲自编程。**每一步前进、转向、采集等由学生用原指令卡编排。新增知识工具融入这套操作，不以自动导航、预填正确路线或填几个数替代编程。
2. **保留原核心操作。**“选择指令 / 我的程序”两列；点卡片追加；点某步“替换”后选卡片原位替换；× 删除；保留主程序/函数页签、运行、单步、撤销、清空和复位。不要另造上下移动列表或整页代码编辑器。
3. **第 19 课保持原大地图。**保留双码头、桥、缺口和 B 区台阶，不能再缩成几格直线。参数工具在原函数页签中设置规则，每次调用在程序卡片里选择步数。
4. **布局明确。**桌面左侧只有 3D 地图，说明、操作、帮助和证据全部在右侧；窄屏保证可用、不横向溢出。
5. **短文字也要讲明白。**首屏说清“做什么、操作哪里、观察什么”；帮助、Python 和详细证据按需展开。不要长篇必读，也不能只留学生看不懂的术语。
6. **多数课程两阶段足够。**先自己构造，再用变化后的任务独立练习。修复只在知识点需要时必做，例如调试课；工程错误测试不等于学生必须做一轮修复。第 19 课是“连接参数槽 → 距离参数迁移”，不用“自己编 / 修工具 / 换图试”作为统一新命名。
7. **新地形服务知识点。**台阶、传送、开关等要产生可观察、可推理的作用，增加地图变化和新鲜感；不能只是换贴图，也不要一课引入多个新规则。保留有路线选择的大地图。
8. **过关不等于掌握。**控件要真实改变程序和世界；接受多种正确方案。通过关键执行事件检验本课能力，使用强提示后需在等价新任务独立验证；不要用参考答案字符串匹配或界面数字假装执行。

## 给模型什么文件、按什么顺序读

无需一开始读完整项目或全部 31 张工单。先了解基线，再只读当前课涉及的段落与代码。

| 顺序 | 文件 | 用途 |
|---|---|---|
| 1 | [AGENTS.md](AGENTS.md)、[CONTEXT.md](CONTEXT.md) | 仓库规则、当前状态、术语 |
| 2 | [PRD.md](.scratch/core-32-learning-mechanics-v18/PRD.md) | 最新教学要求；先看第 2.3 节，再看当前课及共用验收要求 |
| 3 | [LEVEL_DESIGN.md](.scratch/core-32-learning-mechanics-v18/LEVEL_DESIGN.md) | 地图共用规则和当前课关卡设计 |
| 4 | [ENGINEERING_HANDOFF.md](.scratch/core-32-learning-mechanics-v18/ENGINEERING_HANDOFF.md) | 接入接口、执行与证据契约、已实现和缺口 |
| 5 | [IMPLEMENTATION_PLAN.md](.scratch/core-32-learning-mechanics-v18/IMPLEMENTATION_PLAN.md)、[工单索引](.scratch/core-32-learning-mechanics-v18/issues/README.md) | 工程顺序、依赖和实际实施状态 |
| 6 | [第 17 课工单](.scratch/core-32-learning-mechanics-v18/issues/01-lesson-17.md)、[当前验收](.scratch/core-32-learning-mechanics-v18/lesson-17-acceptance.md) | 已完成实现与技术验收；保留课堂未验证边界 |
| 7 | [第 19 课当前验收](.scratch/core-32-learning-mechanics-v18/lesson-19-acceptance.md) | 保留哪些已验证行为、回归哪些场景 |
| 8 | [第 1 课验收](.scratch/core-32-learning-mechanics-v18/lesson-01-acceptance.md)、[第 2 课验收](.scratch/core-32-learning-mechanics-v18/lesson-02-acceptance.md) | 前两课当前实现、固定反例与浏览器记录 |
| 9 | [第 3 课验收](.scratch/core-32-learning-mechanics-v18/lesson-03-acceptance.md)、[第 4 课验收](.scratch/core-32-learning-mechanics-v18/lesson-04-acceptance.md)、[第 5 课验收](.scratch/core-32-learning-mechanics-v18/lesson-05-acceptance.md) | 路线成本、首次偏离与坐标传送的当前实现 |

历史审查和 `lesson-19-acceptance-v1.8-19.*.md` 可在追查问题时阅读，不作为当前产品规格。旧工单与最新要求冲突时，先修订当前工单再实现，不能直接照旧做。

代码按需查看：

| 文件 | 职责 / 注意事项 |
|---|---|
| `signal-runner-node/structured-lessons.js` | 课程注册及统一接口，当前接入第 1、2、17、19 课 |
| `signal-runner-node/foundation-lessons.js`、`foundation-lessons-ui.js` | 第 1、2 课程序模型、关卡、评估与原卡片界面 |
| `signal-runner-node/navigation-lessons.js`、`navigation-lessons-ui.js` | 第 3–5 课路线、调试、坐标传送模型与界面 |
| `signal-runner-node/parameter-lesson.js`、`parameter-lesson-ui.js` | 第 19 课程序模型、地图、评估与原卡片界面样板 |
| `signal-runner-node/app.js`、`index.html`、`styles.css`、`early-lessons.css` | 共用 3D 舞台、原操作区和布局；避免整套重写 |
| `signal-runner-node/learning-evidence.js`、`early-lessons.js` | 掌握证据、提示隔离、保存和版本迁移；不要照抄第 19 课专用判定 |
| `signal-runner-node/python-runtime-core.js`、`world-rules.js` | 真实执行事件、地形与碰撞；修改后必须回归旧行为 |
| `signal-runner-node/course-data.js`、`v16-course-overrides.js` | 原有课程配置，需要时对照原地图和命名 |
| `scripts/test-python-runtime-core.mjs`、`test-early-lessons.mjs`、`test-learning-v18.mjs` | 已有执行、旧课程和新版课程回归 |

## 第 1–5、17、19 课已完成；下一课等待老师指定

第 1–5 课已按学习顺序完成，其中第 4 课保留调试必做修复，第 5 课接通固定传送教学；第 17、19 课原成果保留。后续可按学习顺序继续第 6 课，或按工程依赖处理第 18 课，但**不自动推进**，等待老师指定。

第 17 课实施前的旧工单曾标有 `ready-for-agent`，以下冲突现已在工单和 `1.8-17.1` 实现中修正：

| 旧工单内容 | 接手时应采用的要求 |
|---|---|
| 用固定 `go_to_A()` 等辅助函数自动导航，学生只设置计数规则 | 学生用原指令卡编排路线；变量规则融入原操作区。程序生成器不能藏一条正确路线 |
| 构造 + 必做修复 + 两张独立图 + 必答解释才掌握 | 本课默认构造 + 一次有实质变化的独立迁移；不把修复、解释表单或第二张图设成固定门槛 |
| 迁移只换输入、保留自动执行顺序 | 保留学生已定义的计数规则，让学生在新地图亲自编排和执行；任务变化须检验同一变量能力 |
| 浏览器验收必须依次走完 G→R→解释→T1/T0 | 浏览器验收实际两阶段；错误、重复采集和零结果继续作为自动验证或可选练习 |

以下契约仍保留：变量从学生选择的初值开始；只有成功采集才应加 1；空采与重复采集不应加；界面分别显示 Python 变量和世界实际收集数；允许合法错误规则真实运行。

`try_collect()`、`check_count(value)` 和计数门已按本课范围实现并通过自动与浏览器验收。旧 `collect()` 的行为保持不变；检查只能判断并开门，不能替学生改正确变量。

修订关卡时要让成功、空采和重复尝试成为可观察的有效检验条件，而不是全程只成功采集、让错误规则也碰巧通过。学生仍决定路线；用任务目标和实际事件证明概念，不能硬编码唯一行走序列。零结果测试继续保留，但“只通过零结果”不能单独证明会成功后更新变量。

## 已执行的首次任务指令（历史记录，不要重复实施）

```text
请接手 CodeQuestPlanet，先阅读根目录 MODEL_HANDOFF.md。
本次只完成第 17 课的 v1.8 改造，保留第 19 课 1.8-19.5 和所有现有未提交修改。

按交接文件的阅读顺序读取必要规格，只深入当前课有关内容，不重新审查整个项目。
先修订 issues/01-lesson-17.md 中已过时的自动导航和强制三阶段要求，
简短说明本课学生要做什么、变量怎样真实运行、两阶段怎样证明掌握，然后直接实施。

保留原“选择指令 / 我的程序”、点击追加、替换、删除、撤销和运行等操作。
每一步路线由学生编排，计数工具融入原操作。桌面左侧只有 3D 地图，其他内容在右侧。
讲解简短明白，地图有足够可玩空间；不要用小直线图、隐藏导航或长篇说明替代关卡设计。
默认构造加一次独立迁移，不强制修复或填写解释。错误与边界仍需工程验证。

遵守结构化课程接口，让生成 Python、真实执行、世界反馈和证据来自同一程序模型。
先明确正常、典型误解、重复/空采、零结果的夹具和预期事件，再实施控件与执行逻辑。
合法错误规则可以运行；不允许只改界面数字、匹配唯一答案或用一次世界成功直接判掌握。

完成后运行 npm test、npm run build 和 git diff --check；
在浏览器通过真实控件完成第 17 课构造、独立迁移、刷新恢复，并回归第 19 课原卡片操作。
不要用注入正确状态代替操作验收。工具不可用时写清未验证项，不能报成通过。

提交 .scratch/core-32-learning-mechanics-v18/lesson-17-acceptance.md，
更新 IMPLEMENTATION_PLAN.md 与当前工单记录。报告修改内容、实际测试结果、试玩地址和未验证项。
一次只交付这一课，不自动推进下一课，不部署，不提交 Git，不清理或覆盖无关修改。
遇到常规实现问题自行解决；仅在规格存在无法自行解决的关键冲突时询问。
```

## 完成以后怎么继续、怎么检查

若本课还未完成，直接发：

```text
继续当前课，从现有代码和验收记录接着做，先指出还差哪几项，再完成它们。
不要重新规划整个项目，不要扩大到下一课；完成后更新交接状态和验收记录。
```

若要另一模型复核，发：

```text
请审查刚完成的第 17 课，只读检查，不改代码。
先读 MODEL_HANDOFF.md 和 lesson-17-acceptance.md，再核对相关规格、代码与实际界面。
重点查：原指令卡是否保留、学生是否亲自编排路线、变量是否真实执行、
错误规则能否被区分、迁移是否有效、强提示是否影响掌握判定、刷新是否保留进度。
同时复查第 19 课是否回归。只列有证据的问题、严重程度和复现步骤，标明未验证项。
不要因为测试通过就宣称教学效果已经得到验证。
```

老师自己试玩时，重点看五件事：学生能否马上知道做什么；操作是否还是熟悉的卡片；错误规则是否产生能看懂的反馈；换图后是否必须继续思考；地图变化是否帮助理解本课知识点。确认这一课再下达下一张工单，不一次让模型重做 32 课。

## 本地运行

在项目根目录执行：

```sh
npm test
npm run build
node signal-runner-node/server.mjs
```

若当前电脑提示找不到 Node/npm，先执行：

```sh
export PATH="/Users/jack/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/bin:$PATH"
```

这是当前电脑的运行时路径，其他电脑使用自己的 Node 环境。已有预览服务时复用，不重复启动。服务端口以终端输出为准。

当前可直接打开本地文件 `signal-runner-node/index.html?preview=1&track=course&lesson=course-XX`，将 `XX` 改为 `01`–`05`、`17` 或 `19`；也可复用本地预览服务的同路径。本地保存验证不代表线上账户和多设备同步已验收。
