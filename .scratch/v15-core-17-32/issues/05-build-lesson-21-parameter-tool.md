# 制作第 21 课参数化工具

**Status:** closed  
**Parent:** [v1.5 共同核心第 17–32 课 Python 实施规格](../PRD.md)

## 交付条件

- 学生可以定义带简单参数的真实 Python 函数。
- `for` 与 `range` 使用形参决定真实移动次数。
- 起始程序因第二次实参错误而无法上传。
- 参数调用表显示每次实际调用收到的值。

## Resolution — 2026-08-12

- `move_steps(steps)` 同时服务 2 格与 4 格路线，函数定义无需复制。
- 起始调用轨迹为 `2 → 2`，修复后为 `2 → 4` 并成功上传。
- 函数调用事件保存学生行号与实际参数，进入最终学习证据。
- 自动与真实页面回归均覆盖错误和正确路径。
