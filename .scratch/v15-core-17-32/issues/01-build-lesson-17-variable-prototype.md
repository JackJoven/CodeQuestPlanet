# 制作第 17 课变量能量舱原型

**Status:** closed  
**Parent:** [v1.5 共同核心第 17–32 课 Python 实施规格](../PRD.md)

## 交付条件

- 第 17 课切换到真实 Python 工作区。
- 学生通过实际赋值让 `collected` 按 `0 → 1 → 2` 变化。
- 执行轨迹包含变量更新行、旧值和新值。
- 变量追踪面板无需只靠颜色即可读懂状态。
- 最终世界状态和 `collected == 2` 同时成立才算成功。
- 第 13–16 课回归测试和整站构建继续通过。

## Resolution — 2026-08-11

- 第 17 课已经切换为真实 Python 编辑器，并采用 v1.5“变量能量舱”主题。
- 运行器会在每次实际赋值后生成带学生行号的变量事件，保存最终变量状态。
- 起始程序能够采集两座信标但只留下 `collected == 1`，系统会明确拒绝把世界完成误记为能力达成。
- 补全第二次更新后，变量追踪表显示 `0 → 1 → 2`，目标文字与颜色共同反馈完成状态。
- 自动测试扩展到 19 个运行场景；第 13–17 课回归、脚本语法和整站构建通过。
- 浏览器验证覆盖起始失败、补全成功、桌面布局、680px 窄屏布局和控制台错误检查。
