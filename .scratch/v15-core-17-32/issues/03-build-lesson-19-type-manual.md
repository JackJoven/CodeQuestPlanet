# 制作第 19 课类型说明书

**Status:** closed  
**Parent:** [v1.5 共同核心第 17–32 课 Python 实施规格](../PRD.md)

## 交付条件

- 提供受控的 `Explorer` 与 `Flyer` Python 类型。
- 类型实例支持属性、方法和点语法，动作继续驱动同一 3D 世界。
- 起始程序因对象能力不匹配而停止，并定位到学生代码行。
- 修复后由正确对象完成采集与上传，对象档案保存类型、能量和动作。
- 第 13–19 课回归继续通过。

## Resolution — 2026-08-11

- 新增受控对象模型；`Explorer` 支持移动、采集和上传，`Flyer` 支持移动与扫描。
- `scout.collect()` 生成带 Scout、Flyer、collect 与学生第 8 行信息的能力错误。
- 改为 `neo.collect()` 后，Neo 完成采集与上传并留下独立对象动作轨迹。
- 对象能力档案同时以文字呈现类型、剩余能量与目标，不依赖颜色判断。
- 自动回归覆盖错误能力边界和正确对象选择。
