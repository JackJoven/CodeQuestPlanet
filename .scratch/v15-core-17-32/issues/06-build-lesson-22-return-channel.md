# 制作第 22 课返回值通道

**Status:** closed  
**Parent:** [v1.5 共同核心第 17–32 课 Python 实施规格](../PRD.md)

## 交付条件

- 学生可以定义并调用真实布尔返回函数。
- 返回值事件显示函数名、学生行号与真实结果。
- 起始程序因布尔方向写反而在危险格失败。
- 修复后 `if` 使用返回值决定开启护盾。

## Resolution — 2026-08-12

- `safe_ahead()` 的 `return` 被真实执行、记录并交回调用表达式。
- 起始程序返回 `True` 并跳过护盾，系统定位到危险移动与错误返回行。
- 修复后危险格前返回 `False`，`if not safe_ahead()` 开启护盾并完成采集。
- 返回值流向图以文字显示调用与返回结果，不依赖颜色。
