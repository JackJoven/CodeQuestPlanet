# 第 1–16 课开发基线

**Recorded:** 2026-08-11  
**Purpose:** 在继续开发第 17–32 课时保留当前可运行状态，不把第 1–16 课误记为最终完成。

## 当前可依赖能力

- 第 1–12 课继续使用现有指令编排与课程专属学习工具。
- 第 13–16 课使用学生 Python 源代码驱动 3D 世界。
- Python 运行器能够执行 `if`、布尔组合、`while` 与课程游戏函数。
- 草稿、成功代码、运行次数和完成进度能够保存。
- 第 13–16 课成功与失败路径已有自动测试。

## 基线验证

- `scripts/test-python-runtime-core.mjs`：通过，覆盖 17 个运行场景及早期地图失败反馈。
- `scripts/build-site.mjs`：通过。
- `app.js`、`course-data.js`、`python-runtime-core.js`：语法检查通过。

## 后续统一收口时处理

- 目标年龄学生自主试用和教师判断验证。
- 第 1–16 课知识讲解、分层提示与迁移挑战的一致性。
- 第 13–16 课脚手架撤离强度和自由编辑比例。
- 学习证据、视觉、移动端、键盘操作和无障碍统一。
- 将现有未提交工作区整理为可发布版本。

## 开发约束

- 第 17–32 课不得改变既有课程 ID 或删除已有学习证据。
- 新运行能力必须兼容第 13–16 课并保持既有测试通过。
- 发现第 1–16 课问题时先记录，除非阻塞后续运行能力，否则不在当前阶段顺手重构。
