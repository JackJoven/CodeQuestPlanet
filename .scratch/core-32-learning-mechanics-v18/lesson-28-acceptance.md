# 第 28 课技术验收 · `1.8-28.2`

日期：2026-09-22。技术验收通过；真实学生试教与整课时长未验证。

## 交付

- 20×13、142 格地图由左右相反朝向的两座 C 形仓库与中央交叉状态桥组成；两机位于各自内湾，迁移交换起点并改变初始能量。
- 同一 `Explorer` 行为由 Atlas/Beta 两个实例复用；位置、能量、货物各自保存在对象状态中。
- 新增最小 `recharge(amount)` 对象动作，运行事件和状态面板共同显示只给 Atlas 充能的结果。

## 固定验证

- Atlas 充能后再移动，最终能量为初值 +1；Beta 只移动，最终为初值 −1；两者分别采集一件货物。
- `beta = atlas` 不会创建 Beta 档案，第二次动作作用于同一实例并失败，不能冒充独立状态。
- 构造与起点/初值变化迁移均通过不同语义指纹和两阶段能力门。
- 本地浏览器确认双 C 仓库、中央交叉桥和 2/2 实例状态正确显示；无横向溢出，控制台无 error。

## 未验证

真实学生对类/实例/别名的口头解释、整课时长、触屏、云端多设备和部署环境未验证。
