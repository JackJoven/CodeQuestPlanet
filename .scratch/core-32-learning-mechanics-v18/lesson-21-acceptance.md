# 第 21 课技术验收 · `1.8-21.3`

日期：2026-09-22。技术验收通过；真实学生试教与整课时长未验证。

## 交付

- 19×13、128 格的 S 形物流岛链由西北/东北/西南/东南四座宽岛与两段错位航道组成；中央清单中枢落在 S 弯折点，不再采用矩形园区骨架。
- 学生编辑、增删和重排 `tasks`，再把当前项连接到 `visit_station(current)`；原指令卡的追加、替换、删除、撤销、运行和单步保持不变。
- 构造使用 `A → C → B`，迁移改为 `C → A` 并镜像站点；实际访问事件与返回调度中心共同取证。

## 固定验证

- 参考模型在构造与迁移阶段都按当前清单顺序访问并完成两阶段能力门。
- 写死 A/B/C 的循环来源不能取得列表遍历证据；空清单安全跳过访问并仍能返回中心，不产生运行错误。
- 浏览器确认 `1.8-21.3` 的真实 3D 画面为 S 形多岛链，与第 22–28 课轮廓不同；原控件和运行证据保持可用。

## 未验证

单项清单的真实课堂操作、学生对 `current` 的口头解释、整课时长、触屏和云端多设备同步未验证。
