# 第 30 课技术验收 · `1.8-30.1`

日期：2026-09-22。技术验收通过；真实学生试教与整课时长未验证。

## 交付

- 18×15 叠层沙漏升降塔，以顶层停靠环、中央共享台和下层机房形成纵向空间骨架。
- `is_platform_docked()` 与 `is_platform_occupied()` 分开记录，读取不推进时间；`wait()` 每次推进一拍。
- 学生构造 `while not (docked and not occupied)`，构造图第 1 拍可入，迁移图第 3 拍可入。

## 固定验证

- 正确方案分别等待 1/3 拍，每拍后重新读取传感器。
- 固定等待两拍在三拍图被拒绝；只查占用不能证明停靠条件。
- 永不靠站场景由明确调用上限安全停止，不授予通行或掌握。

## 未验证

完整浏览器视觉截图受本地 `file://` 浏览器策略阻断；真实学生理解、触屏、云端多设备、整课时长和课堂学习效果未验证。
