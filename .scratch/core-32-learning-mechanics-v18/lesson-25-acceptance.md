# 第 25 课技术验收 · `1.8-25.2`

日期：2026-09-22。技术验收通过；真实学生试教与整课时长未验证。

## 交付

- 构造为 15×11、92 格的阶梯楔形蓝图，迁移扩为 16×11；左上窄、右下宽的逐层外扩边界让行列方向直接可见，不再是完整矩形岛面。
- 学生连接行 `y`、列 `x` 和岩石格，再由同一份 `map_data` 调用 `build_world` 重建真实 3D 世界。
- 构造修改 `(11,2)`，迁移修改 `(13,8)`；二维表、世界网格、俯视图和碰撞共用运行结果。

## 固定验证

- 参考模型在两种尺寸中只改变目标格并完成两阶段能力门。
- 交换 row/column 会越界或改变错误位置，不能通过；目标点均不在对角线上，无法用 x/y 相同绕过。
- 浏览器确认目标 `(11,2)`、15×11 蓝图、阶梯边界和斜向高地均正确显示；无横向溢出或控制台警告。

## 未验证

四角边界的真实课堂操作、学生对表格/世界坐标的口头解释、整课时长、触屏和云端多设备同步未验证。
