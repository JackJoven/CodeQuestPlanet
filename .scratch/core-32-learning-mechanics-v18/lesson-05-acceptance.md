# 第 5 课验收：坐标定位

内容版本：`1.8-05.1`。日期：2026-09-20。未提交、未部署。

## 实现与固定夹具

- 两座岛之间没有桥。学生点选候选 `(x,y)` 出口，再用原卡片编排到入口、显式 `teleport()`、出口后移动与采集。
- 引导要求 `(2,5)`，干扰项 `(5,2)` 也是合法落脚格但位于错误岛；迁移交换为 `(5,2)`，同时起点朝向由东改为西。
- 站上入口不自动传送；一次显式传送只产生一个事件、不经过中间格，传送前后朝向保持。错误出口真实落地，随后路线在错误位置空采。

## 验证

- 自动夹具验证正确 `(2,5)`、交换坐标、显式传送次数、真实落点、东/西朝向保持和迁移能力门。运行器既有无地面、关门、尖刺、占用出口原子失败夹具继续通过。
- 浏览器先选择错误 `(5,2)`，Nova 落在错误岛并于 `(6,2)` 空采；改选 `(2,5)` 后同一程序到 `(3,5)` 成功，朝东不变。迁移选择 `(5,2)` 后到 `(4,2)` 成功，朝西不变，进度 2/2。
- 刷新后 5 张迁移卡、坐标选择与 2/2 证据恢复。390×844 页面宽度 390，无横向溢出。
- 回归第 1、2、17、19 课：基础课只显示主程序；第 17、19 课的“计数工具/前进工具”页签正常。
- 全量结果：Python 65 组、旧课程 345 组、v1.8 101 组通过；构建 66 个资源通过，`git diff --check` 通过。

## 未验证

- 真实学生对 x/y 读序与传送保留朝向的自主理解。
- 边界出口已由底层运行器夹具覆盖，尚未作为正式第三张学生任务展示。
- 约 60 分钟整课时长、线上账户与多设备同步。

试玩：<file:///Users/jack/Documents/Codex/CodeQuestPlanet/signal-runner-node/index.html?preview=1&track=course&lesson=course-05>
