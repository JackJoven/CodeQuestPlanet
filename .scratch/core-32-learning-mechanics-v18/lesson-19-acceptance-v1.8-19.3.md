# 第 19 课验收：恢复原双码头地图

内容版本：`1.8-19.3`。日期：2026-09-17。工作区未提交、未部署。

按用户要求恢复 `1.8-19.1` 的双码头几何布局与地形。参数因果说明、真实 Python、同步高亮和修复/迁移判定继续保留。

历史记录：[原双码头技术样板](lesson-19-acceptance-v1.8-19.1.md)、[曾简化地图的修订](lesson-19-acceptance-v1.8-19.2.md)。这两份是历史版本，不代表当前呈现。

## 本次改变

- 首屏直接进入原双码头：11×10、49 个可通行格，两片 8×3 宽平台、一格连接桥、桥侧缺口、B 码头两级高台和两颗宝石。旋转及不同高台表面的原变式一并恢复。
- 主程序按 A 码头 / 连接路线 / B 码头展示九步。两次函数调用之间的掉头、返回、过桥和两处转向全部可见，按真实事件高亮。
- 返回循环使用学生填写的 A 段步数；桥长固定 4 格。函数仍只负责沿当前朝向前进。多行步骤通过源代码行区间映射，循环内部动作不会失去对应步骤。
- 引导和修复预放可编辑转向，独立任务的两个转向均留空，由学生判断。
- 短实验改为可选帮助，可随时返回主地图；小实验不计整课完成。接通参数的定义可带回尚未设置绑定的主任务。
- 内容升版归档前版学习证据，从原双码头重新开始。B 距离为 0 时入口与宝石共用标记，避免重叠标签。

## 自动验证

| 检查 | 结果 |
|---|---|
| Python 运行器回归 | 65 个场景通过 |
| 旧课程行为回归 | 344 个场景通过 |
| v1.8 学习与播放验证 | 54 组场景通过 |
| 原图约束 | 49 个通行格、桥侧缺口、两级高度与两条台阶边正确 |
| 正常/修复/独立各三种变体 | 两颗宝石、四次真实转向，所有事件都有可见步骤；过桥四格后恰到 B 入口 |
| 参数与转向错误 | 错误输入、两个位置的错误转向真实失败；缺少设置才阻断 |
| 0/4、5/0 | 对应函数 0 次移动、正常返回，随后采集入口宝石 |
| 证据大小 | 新版完整近期尝试及全部持久证明约 245,406 字节，小于 512 KiB |
| 生产构建 | 通过；60 个内容哈希资源校验通过 |

继续覆盖可选实验、版本迁移、强帮助传播、修复前后配对、解释、零次迁移、保存重载及生产播放器暂停/复位/迟到编译等边界。课堂学习效果仍需真实学生试教。

## 浏览器检查

通过真实页面选择框与按钮完成双码头 2/4 任务：两次调用实际为 [2,2,2]、[4,4,4]（输入、循环、移动）；两颗宝石、四次转向，未映射事件为 0。

运行固定 3 的原始坏工具，A 调用记录 [2,3,3]，采集失败；改为 distance 后通过，并完成次数与函数职责说明。独立任务开始时两个转向均未填写。

完成 1/5 独立图时仅有一份迁移证明，未判掌握；再完成 0/4 图，第一次调用移动 0 次并正常返回，两颗宝石均采集，能力门 4/4。刷新后仍显示已掌握并保留两份迁移。该浏览器含前两版档案的进度样本为 404,255 字节，小于 512 KiB；真实云端同步仍未联调。

已检查 1280×800 桌面画面。原平台、缺口、窄桥、高台与两颗宝石可见，地图画布有最小高度，避免九步程序挤扁地图。390×844 窄屏先显示地图，再显示完整程序；页面宽度 390，无横向溢出，临时视口已恢复。临时验收空间已清理；应用内保留主地图供用户查看。
