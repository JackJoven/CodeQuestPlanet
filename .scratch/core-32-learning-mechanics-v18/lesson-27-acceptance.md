# 第 27 课技术验收 · `1.8-27.2`

日期：2026-09-22。技术验收通过；真实学生试教与整课时长未验证。

## 交付

- 19×13、119 格地图采用“上方塔楼 + 垂直职责轴 + 下方月牙港”构图；高台与 U 形货港上下分层，迁移交换两端任务区。
- 学生分别给侦察和采集方法选择接收对象；生成的真实 Python 创建 `Flyer` 与 `Explorer` 实例并调用对应方法。
- 对象能力错误由运行时产生，不由界面预判或静态文案模拟。

## 固定验证

- `Scout.scan()` 与 `Transport.collect()` 在构造和换位迁移中均留下对象动作证据。
- `Scout.collect()` 真实触发 object-capability 错误，货物状态不成立，不能通过。
- 地图轮廓、台地高度和任务区域与第 26、28 课不同。
- 本地浏览器参考程序完成侦察与 1 件货物采集；3D 画面清楚区分高台、中央场和低层泊位，无横向溢出。

## 未验证

真实学生对接收者/能力归属的语言解释、整课时长、触屏、云端多设备和部署环境未验证。
