# 第 19 课验收：恢复原指令卡与两阶段流程

内容版本：`1.8-19.5`。日期：2026-09-19。未提交、未部署。

历史版本见 [1.8-19.4](lesson-19-acceptance-v1.8-19.4.md)。当前以用户最新要求为准：保留核心操作形式，将参数工具融入原编程区，不固定要求修复阶段。

## 已实现

- 直接复用原 `operationPanel`：左列“选择指令”、右列“我的程序”、主程序/工具页签及原运行工具栏。
- 点击追加卡片，点某步“替换”后再选卡片原位替换，× 删除，保留撤销、清空、单步和复位；不再提供上一版插入/上下移动列表。
- 前进工具在原函数页签中选择循环次数来源；工具卡像其他指令一样加入主程序，每条调用单独选择步数，源代码与真实执行一致。
- 阶段命名沿用课程具体操作的方式：“连接参数槽 / 距离参数迁移”。构造加一次独立迁移即可完成，无必做修复或额外说明填写；零步等图为可选扩展。
- 保留原双码头与左图右操作布局。新版本归档旧证据，主任务从空白程序开始。
- PRD 更新为默认构造与迁移两阶段，按知识点决定是否必做修复；工程错误和边界夹具仍保留。

## 自动验证

v1.8 的 63 组测试通过，覆盖卡片追加/替换/删除/撤销、上限时可替换、参数实参、正常与错误路线、可选零步边界、无修复完成、强提示隔离、版本迁移、保存恢复、真实播放器暂停/单步/复位。当前完整近期记录与证明样本为 174,488 字节，小于 512 KiB。

Python 运行器 65 个场景、旧课程 344 个场景回归通过；生产构建与 60 个内容哈希资源校验通过，git diff --check 通过。执行命令为项目 scripts 下的 test-python-runtime-core.mjs、test-early-lessons.mjs、test-learning-v18.mjs、build-site.mjs、verify-site-build.mjs。

## 浏览器验证

通过原控件实际点卡片：[move,right]，替换首条后成为 [collect,right]；删除并撤销恢复两条；清空并撤销也恢复两条。函数页签选 distance，回主程序逐条构造 14 条指令完成 2/4 双码头；全部事件映射到卡片，两颗宝石收齐，只有构造有效时未判掌握。

直接进入距离参数迁移，列表为空。从卡片构造另一种 10 条方案，以 walk(1) 返回、walk(4) 过桥、walk(5) 到新目标。四次工具调用被正确接受；仅一份独立迁移即可 2/2、mastered=true，repairEvidence 和 explanationEvidence 均为空。刷新后 10 条程序和学习记录保留。含旧版档案的实际进度为 387,293 字节，小于 512 KiB。

已检查 1280×800 桌面：原地图完整，原卡片双列与工具栏可用；参数卡显示 walk(2) 及卡片内步数，函数页签明确显示工具规则。

390×844 下页面宽度 390，卡片与参数控件均在视口内，无横向溢出。第 19 课切到第 18 课，旧指令卡和工具栏正常；回到第 19 课，参数卡和 2/2 进度正常。临时视口已恢复，临时验收空间已关闭，应用内留空白程序供试玩。

真实学生试教、整课时长、云端多设备联调尚未验证。其余 31 课未在本次实施。
